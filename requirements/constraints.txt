# Shared pins. Every service resolves against this file.
#
# This is what prevents two services resolving FastAPI to different versions —
# a class of bug that only appears at the integration boundary and is miserable
# to diagnose. Never pin a shared library in a service; pin it here.
#
# Regenerate compiled files with:
#   pip-compile --constraint requirements/constraints.txt requirements/<role>.in

fastapi>=0.115,<1.0
pydantic>=2.9,<3.0
pydantic-settings>=2.5,<3.0
sqlalchemy>=2.0.35,<2.1
alembic>=1.13,<2.0
httpx>=0.27,<1.0
redis>=5.0,<6.0
